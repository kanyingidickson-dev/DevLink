services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: devlink
      POSTGRES_PASSWORD: devlink
      POSTGRES_DB: devlink
    ports:
      - "5433:5432"
    volumes:
      - devlink_pgdata:/var/lib/postgresql/data

volumes:
  devlink_pgdata:
